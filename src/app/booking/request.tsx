import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';
export default function BookingRequest() {
  return (
    <Screen>
      <Text>Booking request placeholder</Text>
    </Screen>
  );
}
