import { Text } from 'react-native';
import { Screen } from '../../components/ui/Screen';
export default function PatientBookings() {
  return (
    <Screen>
      <Text>Patient bookings placeholder</Text>
    </Screen>
  );
}
